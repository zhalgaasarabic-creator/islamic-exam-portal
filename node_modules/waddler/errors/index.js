class WaddlerQueryError extends Error {
  constructor(query, params, cause) {
    super(`Failed query: ${query}
params: ${params}`);
    this.query = query;
    this.params = params;
    this.cause = cause;
    Error.captureStackTrace(this, WaddlerQueryError);
    if (cause) this.cause = cause;
  }
}
export {
  WaddlerQueryError
};
//# sourceMappingURL=index.js.map