import { WaddlerQueryError } from "../errors/index.js";
import { SQLTemplate } from "../sql-template.js";
class GelSQLTemplate extends SQLTemplate {
  constructor(sqlWrapper, client, dialect, configOptions, options = { rowMode: "object" }) {
    super(sqlWrapper, dialect, configOptions);
    this.sqlWrapper = sqlWrapper;
    this.client = client;
    this.options = options;
  }
  async execute() {
    const { sql: query, params } = this.sqlWrapper.getQuery(this.dialect);
    let rows;
    try {
      rows = await (this.options.rowMode === "array" ? this.client.withSQLRowMode("array").querySQL(query, params.length ? params : void 0) : this.client.querySQL(query, params.length ? params : void 0));
    } catch (error) {
      throw new WaddlerQueryError(query, params, error);
    }
    this.logger.logQuery(query, params);
    return rows;
  }
  /**
   * For now, throws the Error.
   * Current implementation (a placeholder) will be replaced once gel acquires streaming support or when a suitable third-party solution is found.
   */
  // eslint-disable-next-line require-yield
  async *stream() {
    throw new Error("stream is not implemented for gel yet.");
  }
}
export {
  GelSQLTemplate
};
//# sourceMappingURL=session.js.map