import { Client } from 'gel';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../index-F9h1xSok.js';
import { GelDialect } from './gel-core/dialect.js';
import '@clickhouse/client/';
import '../logger.js';
import 'pg-query-stream';

declare class GelSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: Client;
    private options;
    constructor(sqlWrapper: SQLWrapper, client: Client, dialect: GelDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once gel acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

export { GelSQLTemplate };
