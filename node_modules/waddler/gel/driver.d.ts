import { Client, ConnectOptions } from 'gel';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../logger.js';
import 'pg-query-stream';

interface GelSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: GelSQLQuery;

declare function waddler<TClient extends Client = Client>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    WaddlerConfig & ({
        connection: string | ConnectOptions;
    } | {
        client: TClient;
    })
]): SQL;

export { type GelSQLQuery, sql, waddler };
