import { D as Dialect, o as IdentifierObject, q as Value, I as Identifier, d as SQLIdentifier, r as Values, e as SQLValues, s as Raw, f as SQLRaw, g as SQLDefault } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

declare class GelDialect extends Dialect {
    escapeParam(lastParamIdx: number): string;
    escapeIdentifier(identifier: string): string;
    checkIdentifierObject(object: IdentifierObject): void;
    valueToSQL({ value, lastParamIdx, params }: {
        value: Value;
        lastParamIdx: number;
        params: Value[] | Record<string, any>;
    }): string;
}
declare const SQLFunctions: {
    identifier: (value: Identifier<IdentifierObject>) => SQLIdentifier<IdentifierObject>;
    values: (value: Values) => SQLValues;
    raw: (value: Raw) => SQLRaw;
    default: SQLDefault;
};

export { GelDialect, SQLFunctions };
