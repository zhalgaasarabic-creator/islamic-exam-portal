import { SQL as SQL$1, SQLOptions } from 'bun';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

interface BunSqlSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: BunSqlSQLQuery;

declare function waddler<TClient extends SQL$1 = SQL$1>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection: string | ({
            url?: string;
        } & SQLOptions);
    } | {
        client: TClient;
    }))
]): SQL;

export { type BunSqlSQLQuery, sql, waddler };
