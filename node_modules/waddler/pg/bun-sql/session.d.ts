import { SQL } from 'bun';
import { PgDialect } from '../pg-core/dialect.js';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

declare class BunSqlSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: SQL;
    private options;
    constructor(sqlWrapper: SQLWrapper, client: SQL, dialect: PgDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once bun's postgres driver acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

export { BunSqlSQLTemplate };
