import pg, { PoolClient, Client, PoolConfig } from 'pg';
import { j as SQL, n as SQLParamType, a as SQLQuery, x as WaddlerConfigWithExtensions } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

type NodePgClient = pg.Pool | PoolClient | Client;
interface NodePgSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: NodePgSQLQuery;

declare function waddler<TClient extends NodePgClient>(...params: [
    string
] | [
    string,
    WaddlerConfigWithExtensions
] | [
    (WaddlerConfigWithExtensions & ({
        connection: string | PoolConfig;
    } | {
        client: TClient;
    }))
]): SQL;

export { type NodePgClient, type NodePgSQLQuery, sql, waddler };
