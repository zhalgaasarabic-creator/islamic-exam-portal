import { NodePgClient } from './driver.cjs';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../../index-GJ4oisb0.cjs';
import { PgDialect } from '../pg-core/dialect.cjs';
import 'pg';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

declare class NodePgSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: NodePgClient;
    private options;
    private queryConfig;
    private rawQueryConfig;
    constructor(sqlWrapper: SQLWrapper, client: NodePgClient, dialect: PgDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<any, void, unknown>;
}

export { NodePgSQLTemplate };
