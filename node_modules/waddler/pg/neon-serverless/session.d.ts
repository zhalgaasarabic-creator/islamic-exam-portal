import { Pool, PoolClient, Client } from '@neondatabase/serverless';
import { PgDialect } from '../pg-core/dialect.js';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

type NeonClient = Pool | PoolClient | Client;
declare class NeonServerlessSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: NeonClient;
    private options;
    private rawQueryConfig;
    private queryConfig;
    constructor(sqlWrapper: SQLWrapper, client: NeonClient, dialect: PgDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<any, void, unknown>;
}

export { type NeonClient, NeonServerlessSQLTemplate };
