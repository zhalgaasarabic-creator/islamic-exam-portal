import { Pool, PoolConfig } from '@neondatabase/serverless';
import { j as SQL, n as SQLParamType, a as SQLQuery, x as WaddlerConfigWithExtensions } from '../../index-F9h1xSok.js';
import { NeonClient } from './session.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';
import '../pg-core/dialect.js';

interface NeonServerlessSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: NeonServerlessSQLQuery;

declare function waddler<TClient extends NeonClient = Pool>(...params: [
    string
] | [
    string,
    WaddlerConfigWithExtensions
] | [
    (WaddlerConfigWithExtensions & ({
        connection: string | PoolConfig;
    } | {
        client: TClient;
    }) & {
        ws?: any;
    })
]): SQL;

export { type NeonServerlessSQLQuery, sql, waddler };
