import { Sql } from 'postgres';
import { PgDialect } from '../pg-core/dialect.cjs';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

declare class PostgresSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: Sql;
    private options;
    constructor(sqlWrapper: SQLWrapper, client: Sql, dialect: PgDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once Postgre.js acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

export { PostgresSQLTemplate };
