import { Sql, Options, PostgresType } from 'postgres';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

interface PostgresSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: PostgresSQLQuery;

declare function waddler<TClient extends Sql>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection: string | ({
            url?: string;
        } & Options<Record<string, PostgresType>>);
    } | {
        client: TClient;
    }))
]): SQL;

export { type PostgresSQLQuery, sql, waddler };
