import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-F9h1xSok.js';
import { XataHttpClient } from './session.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';
import '@xata.io/client';
import '../pg-core/dialect.js';

interface XataHttpSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: XataHttpSQLQuery;

declare function waddler({ client, config }: {
    client: XataHttpClient;
    config?: WaddlerConfig;
}): SQL;

export { type XataHttpSQLQuery, sql, waddler };
