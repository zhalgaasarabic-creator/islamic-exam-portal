import { PGlite, PGliteOptions } from '@electric-sql/pglite';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

interface PGliteSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: PGliteSQLQuery;

declare function waddler<TClient extends PGlite = PGlite>(...params: [] | [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection?: (PGliteOptions & {
            dataDir?: string;
        }) | string;
    } | {
        client: TClient;
    }))
]): SQL;

export { type PGliteSQLQuery, sql, waddler };
