import { NeonQueryFunction, HTTPTransactionOptions } from '@neondatabase/serverless';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

interface NeonHttpSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: NeonHttpSQLQuery;

declare function waddler<TClient extends NeonQueryFunction<any, any> = NeonQueryFunction<false, false>>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection: string | ({
            connectionString: string;
        } & HTTPTransactionOptions<boolean, boolean>);
    } | {
        client: TClient;
    }))
]): SQL;

export { type NeonHttpSQLQuery, sql, waddler };
