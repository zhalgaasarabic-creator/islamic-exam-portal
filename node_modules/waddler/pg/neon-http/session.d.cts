import { NeonQueryFunction } from '@neondatabase/serverless';
import { i as SQLTemplate, k as SQLWrapper, D as Dialect, h as SQLTemplateConfigOptions } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

type NeonHttpClient = NeonQueryFunction<any, any>;
declare class NeonHttpSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: NeonHttpClient;
    private options;
    constructor(sqlWrapper: SQLWrapper, client: NeonHttpClient, dialect: Dialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once @neondatabase/serverless acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

export { type NeonHttpClient, NeonHttpSQLTemplate };
