import { VercelPool, VercelClient, VercelPoolClient } from '@vercel/postgres';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../../index-F9h1xSok.js';
import { PgDialect } from '../pg-core/dialect.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

type VercelPgClient = VercelPool | VercelClient | VercelPoolClient;
declare class VercelPgSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: VercelPgClient;
    private options;
    private rawQueryConfig;
    private queryConfig;
    constructor(sqlWrapper: SQLWrapper, client: VercelPgClient, dialect: PgDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<any, void, unknown>;
}

export { type VercelPgClient, VercelPgSQLTemplate };
