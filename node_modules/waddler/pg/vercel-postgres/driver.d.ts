import { sql as sql$1 } from '@vercel/postgres';
import { j as SQL, n as SQLParamType, a as SQLQuery, x as WaddlerConfigWithExtensions } from '../../index-F9h1xSok.js';
import { VercelPgClient } from './session.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';
import '../pg-core/dialect.js';

interface VercelPgSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: VercelPgSQLQuery;

declare function waddler<TClient extends VercelPgClient = typeof sql$1>(...params: [] | [
    WaddlerConfigWithExtensions
] | [
    (WaddlerConfigWithExtensions & ({
        client?: TClient;
    }))
]): SQL;

export { type VercelPgSQLQuery, sql, waddler };
