"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var recycling_pool_exports = {};
__export(recycling_pool_exports, {
  RecyclingPool: () => RecyclingPool,
  createRecyclingPool: () => createRecyclingPool
});
module.exports = __toCommonJS(recycling_pool_exports);
var import_DefaultEvictor = require("./pool-ts/DefaultEvictor.cjs");
var import_Deque = require("./pool-ts/Deque.cjs");
var import_Pool = require("./pool-ts/Pool.cjs");
var import_PriorityQueue = require("./pool-ts/PriorityQueue.cjs");
class RecyclingPool extends import_Pool.Pool {
  recycleTimeout;
  recycleJitter;
  constructor(factory, options) {
    super(import_DefaultEvictor.DefaultEvictor, import_Deque.Deque, import_PriorityQueue.PriorityQueue, factory, options);
    this.recycleTimeout = options.recycleTimeout ??= 9e5;
    this.recycleJitter = options.recycleJitter ??= 6e4;
  }
  async release(resource) {
    const loan = this._resourceLoans.get(resource);
    const createdAt = loan === void 0 ? 0 : loan.pooledResource.creationTime;
    if (new Date(createdAt + this.recycleTimeout - Math.random() * this.recycleJitter) <= /* @__PURE__ */ new Date() && this._count - 1 >= this.min) {
      return this.destroy(resource);
    }
    return super.release(resource);
  }
}
function createRecyclingPool(factory, options) {
  return new RecyclingPool(factory, options);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  RecyclingPool,
  createRecyclingPool
});
//# sourceMappingURL=recycling-pool.cjs.map