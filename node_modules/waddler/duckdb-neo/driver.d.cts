import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../logger.cjs';
import 'pg-query-stream';

interface DuckdbNeoSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: DuckdbNeoSQLQuery;

declare function waddler({ dbUrl, url, min, max, accessMode, maxMemory, threads, logger, }: {
    /** @deprecated */
    dbUrl?: string;
    url: string;
    min?: number;
    max?: number;
    accessMode?: 'read_only' | 'read_write';
    maxMemory?: string;
    threads?: string;
} & WaddlerConfig): SQL;

export { type DuckdbNeoSQLQuery, sql, waddler };
