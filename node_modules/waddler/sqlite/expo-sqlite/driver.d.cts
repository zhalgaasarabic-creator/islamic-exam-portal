import { SQLiteDatabase } from 'expo-sqlite';
import { E as ExpoSqliteSQLTemplate, a as SqliteIdentifierObject, U as UnsafePromise } from '../../index-F40GB_MM.cjs';
import { j as SQL, R as RowData, n as SQLParamType, I as Identifier, d as SQLIdentifier, U as UnsafeParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-GJ4oisb0.cjs';
import 'better-sqlite3';
import 'bun:sqlite';
import '@libsql/client';
import '@op-engineering/op-sqlite';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

interface ExpoSqliteSQL extends Omit<SQL, 'default' | 'unsafe' | 'identifier'> {
    /**
     * sql.default is not implemented for sqlite because sqlite doesn't support feature of specifying 'default' keyword in insert statements.
     */
    <T = RowData>(strings: TemplateStringsArray, ...params: SQLParamType[]): ExpoSqliteSQLTemplate<T>;
    identifier(value: Identifier<SqliteIdentifierObject>): SQLIdentifier<SqliteIdentifierObject>;
    unsafe<RowMode extends 'array' | 'object'>(query: string, params?: UnsafeParamType[], options?: {
        rowMode: RowMode;
    }): UnsafePromise<RowMode extends 'array' ? any[] : {
        [columnName: string]: any;
    }, ExpoSqliteSQLTemplate<any>>;
}
interface ExpoSqliteSQLQuery extends Pick<SQL, 'raw' | 'values'>, Pick<ExpoSqliteSQL, 'identifier'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: ExpoSqliteSQLQuery;

declare function waddler({ client, config }: {
    client: SQLiteDatabase;
    config?: WaddlerConfig;
}): ExpoSqliteSQL;

export { type ExpoSqliteSQL, type ExpoSqliteSQLQuery, sql, waddler };
