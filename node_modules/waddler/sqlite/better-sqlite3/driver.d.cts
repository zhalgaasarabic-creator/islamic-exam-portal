import { Options, Database } from 'better-sqlite3';
import { j as SQL, R as RowData, n as SQLParamType, I as Identifier, d as SQLIdentifier, U as UnsafeParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-GJ4oisb0.cjs';
import { B as BetterSqlite3SQLTemplate, a as SqliteIdentifierObject, U as UnsafePromise } from '../../index-F40GB_MM.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';
import 'bun:sqlite';
import '@libsql/client';
import 'expo-sqlite';
import '@op-engineering/op-sqlite';

interface BetterSqlite3SQL extends Omit<SQL, 'default' | 'unsafe' | 'identifier'> {
    /**
     * sql.default is not implemented for sqlite because sqlite doesn't support feature of specifying 'default' keyword in insert statements.
     */
    <T = RowData>(strings: TemplateStringsArray, ...params: SQLParamType[]): BetterSqlite3SQLTemplate<T>;
    identifier(value: Identifier<SqliteIdentifierObject>): SQLIdentifier<SqliteIdentifierObject>;
    unsafe<RowMode extends 'array' | 'object'>(query: string, params?: UnsafeParamType[], options?: {
        rowMode: RowMode;
    }): UnsafePromise<RowMode extends 'array' ? any[] : {
        [columnName: string]: any;
    }, BetterSqlite3SQLTemplate<any>>;
}
interface BetterSqlite3SQLQuery extends Pick<SQL, 'raw' | 'values'>, Pick<BetterSqlite3SQL, 'identifier'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: BetterSqlite3SQLQuery;

type BetterSQLite3DatabaseConfig = ({
    source?: string | Buffer;
} & Options) | string | undefined;
declare function waddler(...params: [] | [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection?: BetterSQLite3DatabaseConfig;
    } | {
        client: Database;
    }))
]): BetterSqlite3SQL;

export { type BetterSQLite3DatabaseConfig, type BetterSqlite3SQL, type BetterSqlite3SQLQuery, sql, waddler };
