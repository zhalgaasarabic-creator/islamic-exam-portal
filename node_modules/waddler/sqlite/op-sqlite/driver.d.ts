import { DB } from '@op-engineering/op-sqlite';
import { O as OpSqliteSQLTemplate, a as SqliteIdentifierObject, U as UnsafePromise } from '../../index-DnPDMxNu.js';
import { j as SQL, R as RowData, n as SQLParamType, I as Identifier, d as SQLIdentifier, U as UnsafeParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-F9h1xSok.js';
import 'better-sqlite3';
import 'bun:sqlite';
import '@libsql/client';
import 'expo-sqlite';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

interface OpSqliteSQL extends Omit<SQL, 'default' | 'unsafe'> {
    /**
     * sql.default is not implemented for sqlite because sqlite doesn't support feature of specifying 'default' keyword in insert statements.
     */
    <T = RowData>(strings: TemplateStringsArray, ...params: SQLParamType[]): OpSqliteSQLTemplate<T>;
    identifier(value: Identifier<SqliteIdentifierObject>): SQLIdentifier<SqliteIdentifierObject>;
    unsafe<RowMode extends 'array' | 'object'>(query: string, params?: UnsafeParamType[], options?: {
        rowMode: RowMode;
    }): UnsafePromise<RowMode extends 'array' ? any[] : {
        [columnName: string]: any;
    }, OpSqliteSQLTemplate<any>>;
}
interface OpSqliteSQLQuery extends Pick<SQL, 'raw' | 'values'>, Pick<OpSqliteSQL, 'identifier'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: OpSqliteSQLQuery;

declare function waddler({ client, config }: {
    client: DB;
    config?: WaddlerConfig;
}): OpSqliteSQL;

export { type OpSqliteSQL, type OpSqliteSQLQuery, sql, waddler };
