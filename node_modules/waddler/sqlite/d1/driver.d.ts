import { D1Database as D1Database$1 } from '@miniflare/d1';
import { D as D1SQLTemplate, a as SqliteIdentifierObject, U as UnsafePromise } from '../../index-DnPDMxNu.js';
import { j as SQL, R as RowData, n as SQLParamType, I as Identifier, d as SQLIdentifier, U as UnsafeParamType, a as SQLQuery, u as IfNotImported, W as WaddlerConfig } from '../../index-F9h1xSok.js';
import 'better-sqlite3';
import 'bun:sqlite';
import '@libsql/client';
import 'expo-sqlite';
import '@op-engineering/op-sqlite';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

interface D1SQL extends Omit<SQL, 'default' | 'unsafe' | 'identifier'> {
    /**
     * sql.default is not implemented for sqlite because sqlite doesn't support feature of specifying 'default' keyword in insert statements.
     */
    <T = RowData>(strings: TemplateStringsArray, ...params: SQLParamType[]): D1SQLTemplate<T>;
    identifier(value: Identifier<SqliteIdentifierObject>): SQLIdentifier<SqliteIdentifierObject>;
    unsafe<RowMode extends 'array' | 'object'>(query: string, params?: UnsafeParamType[], options?: {
        rowMode: RowMode;
    }): UnsafePromise<RowMode extends 'array' ? any[] : {
        [columnName: string]: any;
    }, D1SQLTemplate<any>>;
}
interface D1SQLQuery extends Pick<SQL, 'raw' | 'values'>, Pick<D1SQL, 'identifier'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: D1SQLQuery;

type AnyD1Database = IfNotImported<D1Database, D1Database$1, D1Database | IfNotImported<D1Database$1, never, D1Database$1>>;
declare function waddler<TClient extends AnyD1Database = AnyD1Database>({ client, config }: {
    client: TClient;
    config?: WaddlerConfig;
}): D1SQL;

export { type AnyD1Database, type D1SQL, type D1SQLQuery, sql, waddler };
