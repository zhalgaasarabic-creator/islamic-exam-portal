import { D as Dialect, o as IdentifierObject, I as Identifier, d as SQLIdentifier, r as Values, e as SQLValues, s as Raw, f as SQLRaw, g as SQLDefault } from '../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../logger.cjs';
import 'pg-query-stream';

declare class DuckdbDialect extends Dialect {
    escapeParam(lastParamIdx: number): string;
    escapeIdentifier(identifier: string): string;
    checkIdentifierObject(object: IdentifierObject): void;
    valueToSQL<DuckdbValue>({ value }: {
        value: DuckdbValue;
    }): string;
}
declare const SQLFunctions: {
    identifier: (value: Identifier<IdentifierObject>) => SQLIdentifier<IdentifierObject>;
    values: (value: Values) => SQLValues;
    raw: (value: Raw) => SQLRaw;
    default: SQLDefault;
};

export { DuckdbDialect, SQLFunctions };
