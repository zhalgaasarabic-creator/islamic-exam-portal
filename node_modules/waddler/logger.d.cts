interface Logger {
    logQuery(query: string, params: unknown[] | Record<string, unknown>, metadata?: any): void;
}
interface LogWriter {
    write(message: string): void;
}
declare class ConsoleLogWriter implements LogWriter {
    write(message: string): void;
}
declare class DefaultLogger implements Logger {
    readonly writer: LogWriter;
    constructor(config?: {
        writer: LogWriter;
    });
    logQuery(query: string, params: unknown[] | Record<string, unknown>, metadata?: any): void;
}
declare class NoopLogger implements Logger {
    logQuery(): void;
}

export { ConsoleLogWriter, DefaultLogger, type LogWriter, type Logger, NoopLogger };
