import { Database } from 'better-sqlite3';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions, D as Dialect, q as Value, I as Identifier, d as SQLIdentifier, r as Values, e as SQLValues, s as Raw, f as SQLRaw } from './index-F9h1xSok.js';
import { Database as Database$1 } from 'bun:sqlite';
import { Client } from '@libsql/client';
import { SQLiteDatabase } from 'expo-sqlite';
import { DB } from '@op-engineering/op-sqlite';

declare class BetterSqlite3SQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: Database;
    private options;
    private returningData;
    constructor(sqlWrapper: SQLWrapper, client: Database, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<BetterSqlite3SQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<BetterSqlite3SQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<Awaited<T>, void, unknown>;
}

declare class BunSqliteSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: Database$1;
    private options;
    private returningData;
    constructor(sqlWrapper: SQLWrapper, client: Database$1, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<BunSqliteSQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<BunSqliteSQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<Awaited<T>, void, unknown>;
}

declare class D1SQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: D1Database;
    private options;
    private returningData;
    constructor(sqlWrapper: SQLWrapper, client: D1Database, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<D1SQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<D1SQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once cloudflare d1 driver acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

declare class DurableSqliteSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: DurableObjectStorage;
    private options;
    private returningData;
    constructor(sqlWrapper: SQLWrapper, client: DurableObjectStorage, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<DurableSqliteSQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<DurableSqliteSQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<Awaited<T>, void, unknown>;
}

declare class LibsqlSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    readonly client: Client;
    options: {
        rowMode: 'array' | 'object';
    };
    returningData: boolean;
    constructor(sqlWrapper: SQLWrapper, client: Client, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<LibsqlSQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<LibsqlSQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once cloudflare d1 driver acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

declare class ExpoSqliteSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: SQLiteDatabase;
    private options;
    private returningData;
    constructor(sqlWrapper: SQLWrapper, client: SQLiteDatabase, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<ExpoSqliteSQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<ExpoSqliteSQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<Awaited<T>, void, unknown>;
}

declare class OpSqliteSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: DB;
    private options;
    private returningData;
    constructor(sqlWrapper: SQLWrapper, client: DB, dialect: SqliteDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    all(): Omit<OpSqliteSQLTemplate<T>, 'all' | 'run'>;
    run(): Omit<OpSqliteSQLTemplate<T>, 'all' | 'run'>;
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once cloudflare op-sqlite driver acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

type SqliteIdentifierObject = {
    table?: string;
    column?: string;
    as?: string;
};
declare class SqliteDialect extends Dialect {
    escapeParam(): string;
    escapeIdentifier(identifier: string): string;
    checkIdentifierObject(object: SqliteIdentifierObject): void;
    valueToSQL({ value, params }: {
        value: Value;
        params: Value[] | Record<string, any>;
    }): string;
}
declare class UnsafePromise<T, DriverT extends D1SQLTemplate<T> | BetterSqlite3SQLTemplate<T> | BunSqliteSQLTemplate<T> | LibsqlSQLTemplate<T> | DurableSqliteSQLTemplate<T> | OpSqliteSQLTemplate<T> | ExpoSqliteSQLTemplate<T>> {
    driver: DriverT;
    constructor(driver: DriverT);
    run(): Omit<UnsafePromise<T, DriverT>, 'run' | 'all'>;
    all(): Omit<UnsafePromise<T, DriverT>, 'run' | 'all'>;
    stream(): AsyncGenerator<Awaited<T>, void, unknown>;
    then<TResult1 = T[], TResult2 = never>(onfulfilled?: ((value: T[]) => TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: ((reason: any) => TResult2 | PromiseLike<TResult2>) | null | undefined): Promise<TResult1 | TResult2>;
}
declare const SQLFunctions: {
    identifier: (value: Identifier<SqliteIdentifierObject>) => SQLIdentifier<SqliteIdentifierObject>;
    values: (value: Values) => SQLValues;
    raw: (value: Raw) => SQLRaw;
};

export { BetterSqlite3SQLTemplate as B, D1SQLTemplate as D, ExpoSqliteSQLTemplate as E, LibsqlSQLTemplate as L, OpSqliteSQLTemplate as O, SqliteDialect as S, UnsafePromise as U, type SqliteIdentifierObject as a, BunSqliteSQLTemplate as b, DurableSqliteSQLTemplate as c, SQLFunctions as d };
