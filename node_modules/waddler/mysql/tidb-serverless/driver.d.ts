import { Connection, Config } from '@tidbcloud/serverless';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-F9h1xSok.js';
import '@clickhouse/client/';
import '../../logger.js';
import 'pg-query-stream';

interface TidbServerlessSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: TidbServerlessSQLQuery;

declare function waddler<TClient extends Connection = Connection>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection: string | Config;
    } | {
        client: TClient;
    }))
]): SQL;

export { type TidbServerlessSQLQuery, sql, waddler };
