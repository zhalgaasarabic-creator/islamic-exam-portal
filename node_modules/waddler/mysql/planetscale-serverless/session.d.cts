import { Client, Connection } from '@planetscale/database';
import { i as SQLTemplate, k as SQLWrapper, h as SQLTemplateConfigOptions } from '../../index-GJ4oisb0.cjs';
import { MySQLDialect } from '../mysql-core/dialect.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

declare class PlanetscaleServerlessSQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: Client | Connection;
    private options;
    private rawQueryConfig;
    private queryConfig;
    constructor(sqlWrapper: SQLWrapper, client: Client | Connection, // TODO should I include Transaction here?
    dialect: MySQLDialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    });
    execute(): Promise<T[]>;
    /**
     * For now, throws the Error.
     * Current implementation (a placeholder) will be replaced once planetscale-serverless driver acquires streaming support or when a suitable third-party solution is found.
     */
    stream(): AsyncGenerator<never, void, unknown>;
}

export { PlanetscaleServerlessSQLTemplate };
