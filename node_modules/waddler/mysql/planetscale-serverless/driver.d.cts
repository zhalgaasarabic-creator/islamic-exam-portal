import { Client, Config } from '@planetscale/database';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

interface PlanetscaleServerlessSQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: PlanetscaleServerlessSQLQuery;

declare function waddler<TClient extends Client = Client>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection: string | Config;
    } | {
        client: TClient;
    }))
]): SQL;

export { type PlanetscaleServerlessSQLQuery, sql, waddler };
