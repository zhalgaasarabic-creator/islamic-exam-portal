import { Pool as Pool$1, Connection as Connection$1, PoolOptions } from 'mysql2';
import { Pool, Connection } from 'mysql2/promise';
import { j as SQL, n as SQLParamType, a as SQLQuery, W as WaddlerConfig } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

interface MySql2SQLQuery extends Pick<SQL, 'identifier' | 'raw' | 'default' | 'values'> {
    (strings: TemplateStringsArray, ...params: SQLParamType[]): SQLQuery;
}
declare const sql: MySql2SQLQuery;

type MySql2Client = Pool | Connection | Pool$1 | Connection$1;
declare function waddler<TClient extends MySql2Client>(...params: [
    string
] | [
    string,
    WaddlerConfig
] | [
    (WaddlerConfig & ({
        connection: string | PoolOptions;
    } | {
        client: TClient;
    }))
]): SQL;

export { type MySql2SQLQuery, sql, waddler };
