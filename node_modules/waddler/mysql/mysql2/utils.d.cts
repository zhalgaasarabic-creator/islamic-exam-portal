import { Pool, Connection } from 'mysql2/promise';

type MySql2Client = Pool | Connection;
declare function isPool(client: MySql2Client): client is Pool;
interface CallbackClient {
    promise(): MySql2Client;
}
declare function isCallbackClient(client: any): client is CallbackClient;

export { isCallbackClient, isPool };
