import { Pool, Connection, QueryOptions } from 'mysql2/promise';
import { i as SQLTemplate, k as SQLWrapper, D as Dialect, h as SQLTemplateConfigOptions } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

declare class MySql2SQLTemplate<T> extends SQLTemplate<T> {
    sqlWrapper: SQLWrapper;
    protected readonly client: Pool | Connection;
    private options;
    private queryConfig;
    private rawQueryConfig;
    constructor(sqlWrapper: SQLWrapper, client: Pool | Connection, dialect: Dialect, configOptions: SQLTemplateConfigOptions, options?: {
        rowMode: 'array' | 'object';
    }, queryConfig?: QueryOptions, rawQueryConfig?: QueryOptions);
    execute(): Promise<T[]>;
    stream(): AsyncGenerator<any, void, unknown>;
}

export { MySql2SQLTemplate };
