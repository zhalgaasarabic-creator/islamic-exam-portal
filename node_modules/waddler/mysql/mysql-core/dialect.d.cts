import { D as Dialect, q as Value, I as Identifier, d as SQLIdentifier, r as Values, e as SQLValues, s as Raw, f as SQLRaw, g as SQLDefault } from '../../index-GJ4oisb0.cjs';
import '@clickhouse/client/';
import '../../logger.cjs';
import 'pg-query-stream';

type MySQLIdentifierObject = {
    table?: string;
    column?: string;
    as?: string;
};
declare class MySQLDialect extends Dialect {
    escapeParam(): string;
    escapeIdentifier(identifier: string): string;
    checkIdentifierObject(object: MySQLIdentifierObject): void;
    valueToSQL({ value, params }: {
        value: Value;
        params: Value[] | Record<string, any>;
    }): string;
}
declare const SQLFunctions: {
    identifier: (value: Identifier<MySQLIdentifierObject>) => SQLIdentifier<MySQLIdentifierObject>;
    values: (value: Values) => SQLValues;
    raw: (value: Raw) => SQLRaw;
    default: SQLDefault;
};

export { MySQLDialect, type MySQLIdentifierObject, SQLFunctions };
