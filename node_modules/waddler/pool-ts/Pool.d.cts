import { DefaultEvictor } from './DefaultEvictor.cjs';
import { Deque } from './Deque.cjs';
import { PriorityQueue } from './PriorityQueue.cjs';
import { ResourceLoan } from './ResourceLoan.cjs';
import { Factory, Options } from './types.cjs';
import './PooledResource.cjs';
import './DequeIterator.cjs';
import './DoublyLinkedList.cjs';
import './ResourceRequest.cjs';
import './Deferred.cjs';

declare class Pool<T> {
    private _config;
    private promiseConstructor;
    private _factory;
    private _draining;
    private _started;
    private _waitingClientsQueue;
    private _factoryCreateOperations;
    private _factoryDestroyOperations;
    private _availableObjects;
    private _testOnBorrowResources;
    private _testOnReturnResources;
    private _validationOperations;
    private _allObjects;
    protected _resourceLoans: Map<T, ResourceLoan<T>>;
    private _evictionIterator;
    private _evictor;
    private _scheduledEviction;
    constructor(Evictor: typeof DefaultEvictor, dequeConstructor: typeof Deque, priorityQueueConstructor: typeof PriorityQueue, factory: Factory<T>, options?: Options);
    private _destroy;
    private _applyDestroyTimeout;
    private _testOnBorrow;
    private _dispatchResource;
    private _dispense;
    private _dispatchPooledResourceToNextWaitingClient;
    private _trackOperation;
    private _createResource;
    private _ensureMinimum;
    private _evict;
    private _scheduleEvictorRun;
    private _descheduleEvictorRun;
    start(): Promise<void>;
    acquire(priority?: number): Promise<T>;
    use<T>(fn: (resource: any) => Promise<T>, priority?: number): Promise<T>;
    isBorrowedResource(resource: T): boolean;
    release(resource: T): Promise<void>;
    destroy(resource: T): Promise<void>;
    private _addPooledResourceToAvailableObjects;
    drain(): Promise<void>;
    private __allResourceRequestsSettled;
    private __allResourcesReturned;
    clear(ensureMinimum?: boolean): Promise<void>;
    end(): Promise<void>;
    ready(): Promise<void>;
    get _potentiallyAllocableResourceCount(): number;
    get _count(): number;
    get spareResourceCapacity(): number;
    get size(): number;
    get available(): number;
    get borrowed(): number;
    get pending(): number;
    get max(): number;
    get min(): number;
}

export { Pool };
